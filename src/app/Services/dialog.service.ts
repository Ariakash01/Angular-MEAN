import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class DialogService {

  constructor() { }

  // closeDialog(){
  //   const dialogRef = dialog.open(DialogComponent, {
  //     data: { icon: "delete", title: "Confirm Deletion", color: "red", message: "want to delete" },
  //     autoFocus: false,

  //   });

  // }
}
